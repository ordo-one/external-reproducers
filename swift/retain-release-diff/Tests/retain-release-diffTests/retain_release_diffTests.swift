import XCTest
@testable import retain_release_diff

final class retain_release_diffTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(retain_release_diff().text, "Hello, World!")
    }
}
